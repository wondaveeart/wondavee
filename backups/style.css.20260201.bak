:root {
  --accent: #7AE0FF;
  --font-main: 'Playfair Display', serif;
  --font-display: 'Playfair Display', serif;
  --font-ui: 'Playfair Display', serif;
  --spacing-container: 5%;
  --spacing-section: 6rem;
  --bg-card: #ffffff;
  --bg-dark-editorial: #000;
  --bg-dark-logo-band: #000;
  --color-title: #F2F2F2;
  --color-body: #CFCFCF;
  --color-secondary: #9A9A9A;
  --border-light: 1px solid rgba(255, 255, 255, 0.1);
}

*,
*::before,
*::after {
  box-sizing: border-box !important;
}

@media (max-width: 900px) {

  .grid-2,
  .grid-3,
  .grid-dual-vinyles,
  .grid-editorial-final,
  .contact-grid {
    grid-template-columns: 1fr !important;
    gap: 2rem !important;
  }

  .split-section {
    flex-direction: column !important;
    min-height: auto !important;
  }

  .split-col {
    width: 100% !important;
    padding: 3rem 1.5rem !important;
  }

  .split-col.media img {
    height: 400px !important;
    object-fit: cover !important;
  }
}

body {
  font-family: var(--font-main) !important;
  background: #000 !important;
  color: var(--color-body);
  line-height: 1.7;
  font-weight: 400 !important;
  -webkit-font-smoothing: antialiased;
  text-align: justify;
}

#page-artiste {
  padding-top: 90px !important;
}

.page-view {
  background: #000 !important;
}

h1,
h2,
h3,
h4 {
  font-family: var(--font-display) !important;
  color: var(--color-title);
  margin-bottom: 24px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  font-weight: 500 !important;
}

p {
  font-family: var(--font-main) !important;
  font-size: 1.1rem !important;
  line-height: 1.8 !important;
  font-weight: 400 !important;
  color: var(--color-body);
  margin-bottom: 1.5rem;
  text-align: justify !important;
}

/* --- UTILS: LIGHT SECTIONS (Museum Contrast) --- */
.section-light {
  background: #ffffff !important;
  color: #111111 !important;
}

.section-light h1,
.section-light h2,
.section-light h3,
.section-light h4,
.section-light p,
.section-light a,
.section-light li,
.section-light div,
.section-light span,
.section-light label {
  color: #111111 !important;
}

.section-light .btn-outline {
  color: #111111 !important;
  border-color: #111111 !important;
}

.section-light .btn-outline:hover {
  background: #111111 !important;
  color: #ffffff !important;
}

/* --- COMPONENTS: BUTTONS (Premium Outline Forced) --- */
.btn-outline,
.btn-outline:visited {
  display: inline-flex !important;
  align-items: center !important;
  justify-content: center !important;
  padding: 8px 18px !important;
  border-radius: 8px !important;
  background: transparent !important;
  color: inherit !important;
  font-family: inherit !important;
  font-size: 0.8rem !important;
  line-height: 1 !important;
  font-weight: 500 !important;
  text-transform: uppercase !important;
  letter-spacing: 0.1em !important;
  text-decoration: none !important;
  cursor: pointer !important;
  transition: all 0.25s ease !important;
  white-space: nowrap !important;
  box-sizing: border-box !important;
  overflow: visible !important;
  outline: none !important;
}

.btn-outline:hover {
  background: currentColor !important;
}

/* Theme Awareness for Hover Inversion */
/* Light Backgrounds */
[style*="background: #fff"] .btn-outline,
[style*="background:#fff"] .btn-outline,
.split-section[style*="background: #fff"] .btn-outline,
.split-section[style*="background:#fff"] .btn-outline {
  color: #fff !important;
}

[style*="background: #fff"] .btn-outline:hover,
[style*="background:#fff"] .btn-outline:hover,
.split-section[style*="background: #fff"] .btn-outline:hover,
.split-section[style*="background:#fff"] .btn-outline:hover {
  color: #000 !important;
}

/* Default Dark Tone Buttons (Editorial sections) */
.btn-outline {
  color: var(--color-title) !important;
  border-color: rgba(255, 255, 255, 0.3) !important;
}

.btn-outline:hover {
  background: #fff !important;
  color: #000 !important;
}

.btn-outline:active {
  transform: scale(0.98) !important;
  transition: transform 0.15s ease-out !important;
}

a {
  text-decoration: none;
  color: inherit;
  transition: 0.3s;
}

a:hover {
  opacity: 0.7;
}

/* --- LAYOUT UTILITIES --- */
.container {
  width: 90%;
  max-width: 1200px;
  margin-left: auto !important;
  margin-right: auto !important;
}


.grid-2 {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
}

.grid-3 {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}

/* Grilles Spécifiques Haut de Gamme */
.grid-dual-vinyles {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 5rem;
}

.grid-editorial-final {
  display: grid;
  grid-template-columns: 1fr 2.9fr 1fr;
  gap: 2rem;
  align-items: center;
}



.flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
}

.flex-between {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.grid-2 {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
}

.grid-3 {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}

/* --- HEADER --- */
#main-header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  padding: 20px var(--spacing-container);
  z-index: 1000;
  display: flex;
  justify-content: space-between;
  align-items: center;
  transition: all 0.3s ease;
  background: rgba(255, 255, 255, 0.98) !important;
  border-bottom: 1px solid rgba(0, 0, 0, 0.1);
}

#main-header.scrolled {
  padding: 15px var(--spacing-container);
  background: #ffffff !important;
}

#main-header .logo-main,
#main-header .nav-link,
#main-header .lang-btn,
#main-header .mobile-toggle {
  color: #000 !important;
}

.brand-logo a {
  display: flex;
  align-items: center;
  gap: 12px;
  text-decoration: none;
}

.logo-main {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 1.6rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: #000;
}

.logo-tagline {
  font-family: var(--font-main);
  font-weight: 300;
  font-size: 0.7rem;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: #888;
}

.main-nav {
  margin-left: auto;
}

.main-nav ul {
  display: flex;
  gap: 1rem;
  list-style: none;
}

.nav-link {
  font-family: var(--font-main);
  font-size: 0.8rem;
  color: #000;
  font-weight: 400;
  opacity: 0.7;
  transition: 0.2s;
}

.nav-link:hover,
.nav-link.active {
  opacity: 1;
}

.mobile-toggle {
  display: none;
  font-size: 1.5rem;
  cursor: pointer;
  color: #000;
}

.header-actions {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  margin-left: 2rem;
  z-index: 1001;
}

.lang-btn {
  background: none;
  padding: 4px 10px;
  font-size: 0.7rem;
  font-family: var(--font-main);
  letter-spacing: 0.1em;
  cursor: pointer;
  transition: all 0.2s;
  color: #000;
  border-radius: 2px;
}

.lang-btn:hover {
  background: #000;
  color: #fff;
  border-color: #000;
}

/* --- HERO COMPONENT --- */
#page-home .hero-section {
  position: relative;
  height: 85vh;
  width: 100%;
  display: flex;
  align-items: flex-end;
  justify-content: flex-start !important;
  /* Descente finale subtile (environ la hauteur d'un bouton supplémentaire) */
  padding: 0 clamp(20px, 5vw, 72px) 6vh clamp(20px, 5vw, 72px) !important;
  overflow: hidden;
  box-sizing: border-box !important;
}

.hero-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center bottom;
  /* Priorité au bas de l'image/vidéo pour ne pas couper le logo */
  z-index: 1;
  filter: brightness(0.6);
}

/* Les styles audio sont déplacés plus bas pour éviter les conflits de reset */

#page-home .hero-content {
  position: relative;
  z-index: 2;
  text-align: left !important;
  display: flex !important;
  flex-direction: column !important;
  align-items: flex-start !important;
  max-width: min(680px, calc(100% - 40px)) !important;
  margin: 0 !important;
  padding: 0 !important;
  overflow: visible !important;
  transform: none !important;
}

/* ===== RESET TOTAL HERO VIDEO (LOGO + TEXTE) ===== */
#page-home .hero-section *,
#page-home .hero-section *::before,
#page-home .hero-section *::after {
  filter: none !important;
  box-shadow: none !important;
  text-shadow: none !important;
  mix-blend-mode: normal !important;
  opacity: 1 !important;
  backdrop-filter: none !important;
}

/* Sécurité supplémentaire sur le logo bas droite */
#page-home .hero-section svg,
#page-home .hero-section img {
  filter: none !important;
}

/* ===== HOME HERO : TYPO ÉDITORIALE (PLAYFAIR 48px) ===== */
#page-home .hero-section .hero-content h1 {
  font-family: "Playfair Display", serif !important;
  font-size: 48px !important;
  line-height: 1.05 !important;
  font-weight: 600 !important;
  color: #F2F2F2 !important;
  letter-spacing: -0.015em !important;
  text-transform: none !important;
  text-align: left !important;
  margin: 0 0 0.4rem 0 !important;
  /* Bloc compact et équilibré */
  filter: none !important;
  text-shadow: none !important;
}

#page-home .hero-section .hero-content p {
  font-family: var(--font-main) !important;
  color: #F2F2F2 !important;
  opacity: 1 !important;
  filter: none !important;
  font-size: 1.1rem !important;
  line-height: 1.8 !important;
  font-weight: 400 !important;
  margin: 0 0 2rem 0 !important;
  text-align: left !important;
  max-width: 50ch !important;
  text-shadow: none !important;
}

/* TEST — si le logo est un élément HTML superposé, ceci le cache */
#page-home .hero-section .logo,
#page-home .hero-section .watermark,
#page-home .hero-section .hero-logo,
#page-home .hero-section .corner-logo,
#page-home .hero-section .sound-btn,
#page-home .hero-section .mute-btn {
  display: none !important;
}

/* --- HERO PLAY TOGGLE (SOBRE) --- */
#page-home .hero-section .hero-play-toggle {
  margin-left: 24px;
  /* Espacement équivalent à ~4 lettres */
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  width: 52px;
  height: 52px;
  background: rgba(255, 255, 255, 0.05) !important;
  border: 2px solid #FFFFFF !important;
  border-radius: 50% !important;
  color: #FFFFFF !important;
  cursor: pointer;
  transition: all 0.3s ease !important;
  padding: 0 !important;
  /* Glow intense pour l'effet de luminosité blanche */
  box-shadow: 0 0 20px rgba(255, 255, 255, 0.6), inset 0 0 10px rgba(255, 255, 255, 0.2) !important;
}

#page-home .hero-section .hero-play-toggle:hover {
  transform: scale(1.1);
  background: rgba(255, 255, 255, 0.2) !important;
  box-shadow: 0 0 30px rgba(255, 255, 255, 0.8), inset 0 0 15px rgba(255, 255, 255, 0.3) !important;
  opacity: 1 !important;
}

#page-home .hero-section .hero-play-toggle:active {
  transform: scale(0.98);
}

/* --- GALLERIE MINIMAL VIDEO BUTTON --- */
.btn-gallery-minimal {
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  width: 48px !important;
  height: 48px !important;
  background: rgba(0, 0, 0, 0.1) !important;
  backdrop-filter: blur(10px) !important;
  -webkit-backdrop-filter: blur(10px) !important;
  border: 0.5px solid rgba(255, 255, 255, 0.3) !important;
  border-radius: 50% !important;
  color: #FFFFFF !important;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1) !important;
  padding: 0 !important;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1) !important;
}

.btn-gallery-minimal:hover {
  transform: scale(1.05) !important;
  background: rgba(255, 255, 255, 0.1) !important;
  border-color: rgba(255, 255, 255, 0.8) !important;
  box-shadow: 0 0 20px rgba(255, 255, 255, 0.15) !important;
}

.btn-gallery-minimal .play-icon-wrap svg {
  width: 16px !important;
  height: 16px !important;
  opacity: 0.9;
  transition: opacity 0.3s ease;
}

.btn-gallery-minimal:hover .play-icon-wrap svg {
  opacity: 1;
}

.play-icon-wrap svg {
  width: 20px;
  height: 20px;
  display: block;
  fill: #FFFFFF !important;
}


#page-home .hero-buttons {
  display: flex !important;
  justify-content: flex-start !important;
  gap: 16px !important;
  flex-wrap: wrap !important;
  margin-top: 2rem !important;
}

.scroll-indicator {
  position: absolute;
  bottom: 2rem;
  right: var(--spacing-container);
  animation: bounce 2s infinite;
  opacity: 0.5;
}

@keyframes bounce {

  0%,
  20%,
  50%,
  80%,
  100% {
    transform: translateY(0);
  }

  40% {
    transform: translateY(-10px);
  }

  60% {
    transform: translateY(-5px);
  }
}

/* --- TILES COMPONENT --- */
.tiles-section {
  padding: var(--spacing-section) 0;
}

.tiles-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}

@media (max-width: 900px) {
  .tiles-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }
}

@media (max-width: 600px) {
  .tiles-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }
}

.tile-card {
  position: relative;
  aspect-ratio: 1/1;
  overflow: hidden;
  background: var(--bg-card);
  cursor: pointer;
}

.tile-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.6s ease;
  opacity: 1 !important;
  /* Lumineux par défaut */
  filter: none !important;
}

.tile-content {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  padding: 1.2rem;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.4), transparent);
  transform: translateY(10px);
  transition: all 0.5s cubic-bezier(0.165, 0.84, 0.44, 1);
  opacity: 0.9;
}

.tile-card:hover .tile-img {
  transform: scale(1.05);
  opacity: 1 !important;
}

.tile-card:hover .tile-content {
  transform: translateY(0);
}

.tile-title {
  font-size: 0.85rem;
  font-weight: 600;
  text-transform: uppercase;
  color: #fff !important;
  letter-spacing: 0.18em;
  margin: 0;
  opacity: 1 !important;
  text-shadow: 0 0 15px rgba(255, 255, 255, 0.3), 0 2px 4px rgba(0, 0, 0, 0.5);
}

/* Specific styling for centered 2x row to match 3-col logic */
.grid-centered-items {
  display: flex !important;
  justify-content: center !important;
  gap: 2rem !important;
  margin-top: 2rem !important;
}

.grid-centered-items .tile-card {
  width: calc((100% - 4rem) / 3) !important;
  max-width: 380px !important;
  flex: 0 0 auto !important;
}

@media (max-width: 900px) {
  .grid-centered-items .tile-card {
    width: calc((100% - 2rem) / 2) !important;
    max-width: none !important;
  }
}

@media (max-width: 600px) {
  .tile-title {
    font-size: 0.7rem;
    letter-spacing: 0.15em;
  }

  .tile-content {
    padding: 0.8rem;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.6), transparent);
  }
}


.tile-subtitle {
  font-size: 0.8rem;
  color: var(--accent);
  text-transform: uppercase;
  letter-spacing: 1px;
}

/* --- SPLIT SECTION (Tableaux/Vinyles) --- */
/* --- SPLIT SECTION (Tableaux/Vinyles) --- */
.split-section {
  display: flex;
  min-height: 480px;
  align-items: stretch;
  gap: 0;
  margin: 0;
  padding: 0;
  overflow: visible;
  width: 100%;
}

.split-section.reverse {
  flex-direction: row-reverse;
}

.split-col {
  width: 50%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 4rem 6%;
  box-sizing: border-box;
}

.split-col.media {
  padding: 0;
  position: relative;
}

.split-col.media img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.split-col.text {
  align-items: center;
  text-align: center;
  overflow: visible;
}

/* DESKTOP STRUCTURE LOCK (HOME) */
@media (min-width: 900px) {
  #page-home .split-section {
    display: flex !important;
    flex-direction: row;
    height: 520px !important;
    min-height: 520px !important;
  }

  #page-home .split-section.reverse {
    flex-direction: row-reverse !important;
  }

  #page-home .split-col {
    width: 50% !important;
    height: 100% !important;
  }
}



/* --- ABOUT SECTION --- */
.about-pillars {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
  margin-top: 2rem;
}

.pillar-card {
  background: rgba(255, 255, 255, 0.03);
  padding: 1.5rem;
  text-align: center;
}

.pillar-card h4 {
  font-size: 0.9rem;
  margin-bottom: 0.5rem;
  color: var(--accent);
}

.pillar-card p {
  margin: 0;
}

/* --- GALLERY / WORKS PAGE --- */
.filters-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  margin-bottom: 3rem;
  border-bottom: var(--border-light);
  padding-bottom: 1.5rem;
}

.filter-chip {
  font-size: 0.85rem;
  cursor: pointer;
  padding: 5px 0;
  border-bottom: 2px solid transparent;
  color: var(--text-muted);
}

.filter-chip.active {
  color: var(--text-main);
  border-color: var(--text-main);
}

.gallery-grid-main {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
  gap: 3rem;
}

.artwork-card-full .art-img {
  aspect-ratio: 3/4;
  background: #222;
  margin-bottom: 1rem;
}

.meta-row {
  display: flex;
  justify-content: space-between;
  font-size: 0.8rem;
  border-top: 1px solid #333;
  padding-top: 0.5rem;
  margin-top: 0.5rem;
  color: #777;
}

/* --- CONTACT --- */
.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
}

.form-group {
  margin-bottom: 1.5rem;
}

.form-input,
.form-textarea {
  width: 100%;
  background: transparent;
  border-bottom: 1px solid #333;
  padding: 15px 0;
  color: #fff;
  font-family: var(--font-main);
}

.form-input:focus,
.form-textarea:focus {
  outline: none;
  border-color: var(--accent);
}

/* Old footer styles removed to avoid interference */


/* --- RESPONSIVE --- */
/* --- SPECIFIC LAYOUTS --- */
.grid-3-resp {
  display: grid;
  grid-template-columns: 1fr 0.8fr 1fr;
  gap: 3rem;
  align-items: center;
}

.respire {
  height: 6rem;
  background: transparent !important;
}


/* --- SHOP GALLERY --- */
.shop-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 4rem 2rem;
  margin-top: 2rem;
  margin-bottom: 4rem;
}

.shop-item {
  text-align: left;
  /* Plus élégant qu'au centre pour une galerie */
  transition: transform 0.3s ease;
}

.shop-item img {
  width: 100%;
  aspect-ratio: 1/1;
  object-fit: cover;
  margin-bottom: 1rem;
  display: block;
  border-radius: 2px;
  background: #0a0a0a;
}

.shop-item-title {
  font-size: 1.05rem;
  font-weight: 600;
  letter-spacing: 0.05em;
  margin-bottom: 0.4rem;
  color: #fff;
}

.shop-item-meta {
  font-size: 0.9rem;
  color: #aaa;
  letter-spacing: 0.02em;
}

.shop-item-status {
  font-size: 0.8rem;
  color: #fff;
  letter-spacing: 0.1em;
  margin-left: 1.2rem;
  display: inline-block;
  opacity: 0.6;
}

.shop-item-price {
  font-weight: 500;
  color: #fff;
  letter-spacing: 0.05em;
  margin: 0 1rem;
  opacity: 0.9;
}

/* --- SHOP FILTERS (NEW HIERARCHY) --- */
.shop-filter-main {
  display: flex;
  justify-content: center;
  gap: 3rem;
  margin-bottom: 2rem;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  padding-bottom: 1.2rem;
}

.filter-level-2 {
  margin-bottom: 1.5rem;
  animation: fadeInFilter 0.4s ease forwards;
}

.filter-level-2 .s-filter {
  margin: 0 20px;
  font-size: 0.9rem;
  letter-spacing: 2px;
}

.filter-level-3 {
  margin-top: 1rem;
  margin-bottom: 2rem;
  padding: 1.5rem;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 4px;
  animation: fadeInFilter 0.4s ease forwards;
}

.series-compact-grid {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 12px 24px;
  max-width: 900px;
  margin: 0 auto;
}

@keyframes fadeInFilter {
  from {
    opacity: 0;
    transform: translateY(5px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Base style for filters */
.s-filter {
  cursor: pointer;
  font-family: var(--font-main);
  transition: all 0.3s ease;
  padding-bottom: 4px;
  font-size: 0.85rem;
  opacity: 0.5;
  color: #fff;
  text-transform: uppercase;
}

.s-filter:hover {
  opacity: 1;
}

.s-filter.active-filter {
  opacity: 1 !important;
  font-weight: 600 !important;
  border-bottom: 1px solid #fff;
}

@media (max-width: 1200px) {
  .shop-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (max-width: 900px) {
  .shop-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem 1.5rem;
  }
}

@media (max-width: 600px) {
  .shop-grid {
    grid-template-columns: 1fr;
  }
}

/* --- HOVER IMAGES (GALLERY STYLE) --- */
/* Target artwork placeholders, shop grid, collections, and editorial images */
.shop-grid img,
.tiles-grid img,
.grid-3-resp img,
.grid-3 img,
.split-col.media img,
.contact-photo img,
.marquee-item img,
.tile-card img,
img[onclick] {
  transition: transform 0.2s ease-out, filter 0.2s ease-out !important;
  cursor: pointer;
}

.shop-grid img:hover,
.tiles-grid img:hover,
.grid-3-resp img:hover,
.grid-3 img:hover,
.split-col.media img:hover,
.contact-photo img:hover,
.marquee-item img:hover,
.tile-card img:hover,
img[onclick]:hover {
  transform: scale(1.015) !important;
  filter: brightness(1.02) !important;
}

/* Explicit Exclusions */
.hero-section img,
.logo-band img,
.brand-logo img,
#main-header img {
  transform: none !important;
  filter: none !important;
  cursor: default !important;
}

/* --- SCROLL REVEAL (STYLE GALERIE) --- */
.reveal {
  opacity: 0;
  transform: translateY(10px);
  transition: opacity 0.5s ease-out, transform 0.5s ease-out;
  will-change: opacity, transform;
}

.reveal.visible {
  opacity: 1;
  transform: translateY(0);
}

/* ===== HOME (et global) — Ajustements bas de page SANS toucher aux placeholders ===== */

/* ============================================================
   FOOTER — RESET VISUEL (Constraint E)
   ============================================================ */
.site-footer {
  background: #000000 !important;
  color: #ffffff !important;
  padding: 0 !important;
  /* Managed by sub-elements */
  text-align: center !important;
  border-top: 1px solid rgba(255, 255, 255, 0.05) !important;
}

/* Bandeau Logo - Unifié et architectural */
.footer-logo-band {
  padding: 25px 0 !important;
  border-bottom: 1px solid rgba(255, 255, 255, 0.03) !important;
}

.footer-main-content {
  padding: 20px 0 15px 0 !important;
}









.footer-logo {
  display: flex !important;
  justify-content: center !important;
  align-items: center !important;
  margin: 0 !important;
}

.footer-logo img {
  height: 200px !important;
  width: auto !important;
  display: block !important;
}







/* Conteneurs Menus & RS */
.footer-nav,
.footer-socials {
  margin: 0 !important;
  padding: 0 !important;
}

.footer-links-list {
  list-style: none !important;
  padding: 0 !important;
  margin: 0 0 15px 0 !important;
  display: flex !important;
  justify-content: center !important;
  align-items: center !important;
  gap: 25px !important;
  flex-wrap: wrap !important;
}



/* Style des liens global footer */
.site-footer a {
  color: #ffffff !important;
  text-decoration: none !important;
  font-size: 0.7rem !important;
  letter-spacing: 0.12em !important;
  text-transform: uppercase !important;
  opacity: 0.55 !important;
  transition: opacity 0.3s ease !important;
}

.site-footer a:hover {
  opacity: 0.85 !important;
}

/* Espacement interne rythmé */
.footer-main-content .container>*:not(:last-child) {
  margin-bottom: 12px !important;
}







.footer-bottom {
  border-top: 1px solid rgba(255, 255, 255, 0.05) !important;
  padding-top: 10px !important;
  margin-top: 10px !important;
}





.footer-bottom p {
  font-size: 1.1rem !important;
  opacity: 0.3 !important;
  margin: 0 !important;
}

@media (max-width: 768px) {
  .footer-logo-band {
    padding: 15px 0 !important;
  }

  .footer-main-content {
    padding: 15px 0 10px 0 !important;
  }

  .footer-logo img {
    height: 150px !important;
  }

  .footer-links-list {
    gap: 8px 15px !important;
  }
}








/* Sécurité : aucune influence sur les sections/placeholder */


/* ======================================================
   TYPO HOME + HEADER (FORCÉ) + HERO PLUS GRAND
   ====================================================== */

/* Variables globales (sécurisation effectuée en haut du fichier) */

/* ---------- HEADER : WONDAVEE + ART CONTEMPORAIN ---------- */
/* On cible large pour matcher ton HTML quel qu'il soit */
header#main-header *,
#main-header * {
  font-family: inherit;
}

#main-header .logo,
#main-header .brand,
#main-header .brand-name,
#main-header .site-title,
#main-header a.logo,
#main-header a[aria-label*="logo" i],
#main-header [data-lk*="logo" i],
#main-header [class*="logo" i],
#main-header [class*="brand" i] {
  font-family: var(--font-display) !important;
  font-weight: 500 !important;
  letter-spacing: 0.02em !important;
}

#main-header .tagline,
#main-header .subtitle,
#main-header .brand-subtitle,
#main-header [class*="tagline" i],
#main-header [class*="subtitle" i] {
  font-family: var(--font-ui) !important;
  font-weight: 300 !important;
  letter-spacing: 0.28em !important;
  text-transform: uppercase !important;
}

/* ---------- HERO : Titre + sous-titre (police + taille) ---------- */
/* On vise les éléments vus dans ton inspecteur (h1 + p dans hero-content) */
#page-home .hero-section h1,
#page-home .hero-content h1,
#page-home h1[data-fr],
#page-home h1[data-en] {
  font-family: var(--font-display) !important;
  font-weight: 500 !important;

  /* + grand (≈ +20%) sans exploser sur mobile */
  font-size: clamp(44px, 4.6vw, 86px) !important;
  line-height: 1.05 !important;
}

#page-home .hero-section p,
#page-home .hero-content p,
#page-home p[data-fr],
#page-home p[data-en] {
  font-family: var(--font-ui) !important;
  font-weight: 400 !important;

  /* + grand aussi, propre */
  font-size: clamp(16px, 1.35vw, 22px) !important;
  line-height: 1.35 !important;
}

/* ============================
   HOME — Police unifiée (FORCE)
   - applique la police à TOUT #page-home
   - remet les titres comme avant
   - ne touche pas au header (hors #page-home)
   ============================ */

/* 1) Force la police partout sur la HOME */
#page-home,
#page-home * {
  font-family: var(--font-main) !important;
}

/* 2) Remet les TITRES (ne change ni leur taille, ni leur police d’origine) */
#page-home h1,
#page-home h2,
#page-home h3 {
  font-family: var(--font-display) !important;
  /* ta police titre existante */
}

/* 3) (optionnel) si certains titres utilisent une autre variable chez toi */
#page-home h1[data-fr],
#page-home h2[data-lk] {
  font-family: var(--font-display) !important;
}

/* surtout : header inchangé */
#main-header,
#main-header * {
  font-family: unset !important;
}

/* HOME — rendu éditorial luxe (type Amaury Dubois) */
#page-home p {
  font-size: 1.1rem !important;
  line-height: 1.8 !important;
  letter-spacing: 0.015em !important;
  font-weight: 400 !important;
}

#page-home .split-col.text p {
  max-width: 42ch !important;
  /* colonne éditoriale */
}

/* ===== HOME — DÉCOUVRIR LES ŒUVRES (PLANCHES SÉRIES) ===== */
/* Remarque : Les images sont désormais gérées directement dans le HTML pour plus de fiabilité. */

/* ============================================================ */
/* HEADER LOCK — RESTAURATION DESIGN PRO                        */
/* ============================================================ */

/* Isolation totale du header contre les règles globales */
#main-header {
  all: revert !important;
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
  width: 100% !important;
  height: 80px !important;
  background: #ffffff !important;
  display: flex !important;
  align-items: baseline !important;
  /* Ligne optique de base */
  justify-content: flex-start !important;
  /* Pour contrôler finement les marges auto */
  padding: 28px 5% 0 5% !important;
  /* Centrage optique de la ligne baseline */
  z-index: 9999 !important;
  border-bottom: 1px solid rgba(0, 0, 0, 0.08) !important;
  box-sizing: border-box !important;
}

/* Logo */
#main-header .brand-logo,
#main-header .brand-logo a {
  display: inline-flex !important;
  flex-direction: row !important;
  align-items: baseline !important;
  gap: 8px !important;
  /* Espace réduit entre logo et tagline (-2px) */
  text-decoration: none !important;
  color: #000000 !important;
  background: transparent !important;
}

#main-header .logo-main {
  font-family: 'Playfair Display', serif !important;
  font-size: 28.8px !important;
  font-weight: 700 !important;
  letter-spacing: 0.1em !important;
  text-transform: uppercase !important;
  margin: 0 !important;
  line-height: 1 !important;
}

#main-header .logo-tagline {
  font-family: var(--font-main) !important;
  font-size: 11px !important;
  font-weight: 300 !important;
  letter-spacing: 0.2em !important;
  color: #666666 !important;
  text-transform: uppercase !important;
  margin: 0 0 0 10px !important;
  line-height: 1 !important;
  display: inline-block !important;
  transform: none !important;
}

@media (max-width: 900px) {
  #main-header .logo-tagline {
    display: none !important;
  }
}

/* Navigation */
#main-header .main-nav {
  margin-left: auto !important;
  /* Pousse le menu vers la droite */
  margin-right: 20px !important;
  /* Rapprochement léger du drapeau (-15%) */
  transform: none !important;
}

@media (max-width: 900px) {
  #main-header .main-nav:not(.open) {
    display: none !important;
  }
}

#main-header .main-nav ul {
  display: flex !important;
  list-style: none !important;
  gap: 26px !important;
  margin: 0 !important;
  padding: 0 !important;
  align-items: baseline !important;
}

#main-header .nav-link {
  font-family: var(--font-main) !important;
  font-size: 15px !important;
  font-weight: 400 !important;
  color: #000000 !important;
  text-transform: none !important;
  line-height: 1 !important;
  letter-spacing: 0.02em !important;
  text-decoration: none !important;
  opacity: 0.7 !important;
  background: transparent !important;
  padding-bottom: 4px !important;
  border-bottom: 1px solid transparent !important;
  transition: opacity 0.3s ease, border-color 0.3s ease !important;
}

#main-header .nav-link.active,
#main-header .nav-link:hover {
  opacity: 1 !important;
  border-bottom: 1px solid rgba(0, 0, 0, 0.1) !important;
}

/* Actions Header (Langue/Mobile) */
#main-header .header-actions {
  display: flex !important;
  align-items: baseline !important;
  gap: 10px !important;
  transform: none !important;
}

#main-header .lang-toggle {
  background: transparent !important;
  border: none !important;
  outline: none !important;
  box-shadow: none !important;
  padding: 0 !important;
  cursor: pointer !important;
  display: inline-flex !important;
  align-items: center !important;
  justify-content: center !important;
  line-height: 1 !important;
  transition: opacity 0.2s ease !important;
}

#main-header .lang-toggle:hover {
  opacity: 0.7 !important;
}

#main-header .lang-flag {
  font-size: 18px !important;
  line-height: 1 !important;
  transform: none !important;
}

#main-header .mobile-toggle {
  display: none !important;
  color: #000000 !important;
  font-size: 28px !important;
  cursor: pointer !important;
}

@media (max-width: 900px) {
  #main-header {
    height: 70px !important;
    padding: 0 5% !important;
    align-items: center !important;
    background: rgba(255, 255, 255, 0.95) !important;
    backdrop-filter: blur(10px) !important;
  }

  #main-header .brand-logo {
    order: 2 !important;
    flex: 1 !important;
    text-align: center !important;
    margin: 0 !important;
    display: flex !important;
    justify-content: center !important;
    z-index: 10000 !important;
  }

  #main-header .header-actions {
    order: 3 !important;
    margin: 0 !important;
    flex: 0 0 50px !important;
    display: flex !important;
    justify-content: flex-end !important;
    z-index: 10000 !important;
  }

  #main-header .mobile-toggle {
    display: flex !important;
    flex-direction: column !important;
    justify-content: center !important;
    align-items: flex-start !important;
    order: 1 !important;
    margin: 0 !important;
    flex: 0 0 50px !important;
    height: 40px !important;
    cursor: pointer !important;
    z-index: 10000 !important;
  }

  #main-header .mobile-toggle span {
    display: block !important;
    width: 24px !important;
    height: 1.5px !important;
    background: #000 !important;
    margin: 3px 0 !important;
    transition: transform 0.3s ease, opacity 0.3s ease !important;
    transform-origin: center !important;
  }

  #main-header .mobile-toggle.open span:nth-child(1) {
    transform: translateY(7.5px) rotate(45deg) !important;
  }

  #main-header .mobile-toggle.open span:nth-child(2) {
    opacity: 0 !important;
  }

  #main-header .mobile-toggle.open span:nth-child(3) {
    transform: translateY(-7.5px) rotate(-45deg) !important;
  }

  /* Menu Overlay Refinement - Premium Dark Mode */
  #main-header .main-nav:not(.open) {
    display: none !important;
  }

  #main-header .main-nav.open {
    display: flex !important;
    flex-direction: column !important;
    justify-content: center !important;
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    width: 100% !important;
    height: 100vh !important;
    background: #000000 !important;
    /* Dark theme for premium feel */
    padding: 0 !important;
    margin: 0 !important;
    z-index: 9998 !important;
    overflow-y: auto !important;
    animation: menuFadeIn 0.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  @keyframes menuFadeIn {
    from {
      opacity: 0;
      backdrop-filter: blur(0px);
    }

    to {
      opacity: 1;
      backdrop-filter: blur(10px);
    }
  }

  #main-header .main-nav ul {
    flex-direction: column !important;
    gap: 20px !important;
    padding: 0 !important;
    margin: 0 !important;
    align-items: center !important;
  }

  #main-header .nav-link {
    display: block !important;
    padding: 15px 30px !important;
    font-size: 32px !important;
    font-family: 'Playfair Display', serif !important;
    font-weight: 400 !important;
    letter-spacing: 0.15em !important;
    width: auto !important;
    border-bottom: none !important;
    text-align: center !important;
    color: #ffffff !important;
    /* White text on dark background */
    text-transform: uppercase !important;
    transition: transform 0.3s ease, color 0.3s ease !important;
    opacity: 0;
    /* Hidden initially for stagger */
    animation: linkFadeIn 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  @keyframes linkFadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Stagger Animations */
  #main-header .main-nav.open li:nth-child(1) .nav-link {
    animation-delay: 0.1s;
  }

  #main-header .main-nav.open li:nth-child(2) .nav-link {
    animation-delay: 0.15s;
  }

  #main-header .main-nav.open li:nth-child(3) .nav-link {
    animation-delay: 0.2s;
  }

  #main-header .main-nav.open li:nth-child(4) .nav-link {
    animation-delay: 0.25s;
  }

  #main-header .main-nav.open li:nth-child(5) .nav-link {
    animation-delay: 0.3s;
  }

  #main-header .main-nav.open li:nth-child(6) .nav-link {
    animation-delay: 0.35s;
  }

  #main-header .nav-link.active {
    color: #ffffff !important;
    position: relative;
  }

  #main-header .nav-link.active::after {
    content: '';
    position: absolute;
    bottom: 5px;
    left: 20%;
    right: 20%;
    height: 1px;
    background: rgba(255, 255, 255, 0.3);
  }

  #main-header .nav-link:active {
    transform: scale(0.95) !important;
  }

  /* Ensure header logos and toggles switch color on Dark Menu */
  #main-header .mobile-toggle.open span {
    background: #ffffff !important;
  }

  #main-header .mobile-toggle.open {
    z-index: 10001 !important;
  }



  /* Fallback: use classes or handle in JS if needed, but for now let's hope the fixed header text color works */
  #main-header.menu-open .logo-main {
    color: #ffffff !important;
  }

  #main-header.menu-open .lang-flag {
    filter: invert(1);
  }


  /* Centrage des boutons Hero sur mobile */
  #page-home .hero-buttons {
    justify-content: center !important;
    gap: 12px !important;
  }
}

/* ============================================================ */
/* HOME TYPO — HARMONISATION CONTENU (SANS LE HEADER)           */
/* ============================================================ */

/* Ciblage strict des sections de contenu pour ne pas polluer le header */
/* Le H1 est déjà traité plus haut pour le Hero avec #page-home .hero-section .hero-content h1 */

#page-home .split-col.text h2,
#page-home .split-col.text h3,
#page-home .section-title h2 {
  font-family: 'Playfair Display', serif !important;
  font-size: 26px !important;
  font-weight: 700 !important;
  letter-spacing: 0.1em !important;
  text-transform: uppercase !important;
  text-align: center !important;
  margin-bottom: 2rem !important;
  color: inherit !important;
  text-shadow: none !important;
}

#page-home .split-col.text p,
#page-home .about-text p {
  font-family: var(--font-main) !important;
  font-size: 1.1rem !important;
  line-height: 1.8 !important;
  max-width: 48ch !important;
  margin: 0 auto 1.5rem auto !important;
  text-align: center !important;
  color: inherit !important;
}

/* --- ACCUEIL : HOMOGÉNÉISATION DES TITRES --- */
#page-home .home-block-title {
  font-family: 'Playfair Display', serif !important;
  font-size: 26px !important;
  font-weight: 700 !important;
  letter-spacing: 0.1em !important;
  margin-bottom: 2rem !important;
  /* Alignement sur la règle parente */
  margin-top: 28px !important;
  text-align: center !important;
  line-height: 1.2 !important;
  color: inherit !important;
}


/* Boutons Home */
#page-home .btn,
#page-home .btn-outline {
  font-family: var(--font-main) !important;
  font-size: 13px !important;
  font-weight: 500 !important;
  letter-spacing: 0.1em !important;
  box-shadow: none !important;
  text-shadow: none !important;
}

/* =========================
   CONTACT — CLEAN TYPO + LAYOUT (SANS TOUCHER AU HEADER)
   ========================= */

#page-contact,
#page-contact *:not(#main-header):not(#main-header *):not(footer):not(footer *) {
  font-family: var(--font-main) !important;
  letter-spacing: 0.01em;
}

/* Titres de la page contact : homogènes, sobres */
#page-contact h1,
#page-contact h2,
#page-contact h3 {
  font-family: var(--font-main) !important;
  font-weight: 600 !important;
  letter-spacing: 0.06em !important;
  text-transform: uppercase;
  line-height: 1.2 !important;
  margin: 0 0 18px 0 !important;
}

/* Texte courant : lisible, pas étiré */
#page-contact p,
#page-contact li,
#page-contact label,
#page-contact input,
#page-contact textarea {
  font-size: 16px !important;
  line-height: 1.6 !important;
  font-weight: 400 !important;
}

#page-contact .form-checkbox-group {
  display: flex !important;
  align-items: center !important;
  gap: 12px !important;
  cursor: pointer !important;
  margin: 1.5rem 0 !important;
}

#page-contact .form-checkbox-group input[type="checkbox"] {
  width: 16px !important;
  height: 16px !important;
  margin: 0 !important;
  cursor: pointer !important;
}

#page-contact .form-checkbox-group span {
  font-size: 0.85rem !important;
  line-height: 1 !important;
  user-select: none !important;
}

/* Container global : largeur + respiration */
#page-contact .container {
  max-width: 1100px !important;
}

/* Grille contact : Formulaire+Photo à gauche | Textes à droite */
#page-contact .contact-grid {
  display: grid !important;
  grid-template-columns: 1.05fr 0.95fr !important;
  gap: 56px !important;
  align-items: start !important;
}

#page-contact .contact-left {
  min-width: 0 !important;
}

#page-contact .contact-right {
  min-width: 0 !important;
  padding-top: 14px !important;
}

#page-contact .contact-photo {
  width: 100% !important;
  max-width: 420px !important;
  /* look portrait */
  margin: 32px auto 0 !important;
  /* espacement modéré sous le form */
}

#page-contact .contact-photo img {
  width: 100% !important;
  aspect-ratio: 3 / 4 !important;
  /* portrait */
  height: auto !important;
  max-height: 560px !important;
  object-fit: cover !important;
  object-position: top center !important;
  display: block !important;

  border-radius: 10px !important;
}


/* Espacements Colonne Droite (FAQ + Projets) */
#page-contact .contact-right>div {
  margin-bottom: 1.5rem !important;
  /* Réduction marges entre blocs */
}

#page-contact .faq-item {
  margin-bottom: 0.8rem !important;
}

#page-contact .faq-item p {
  margin-bottom: 0.1rem !important;
  /* Compactage FAQ */
}

/* Marge basse avant logo band */
#page-contact .container {
  padding-bottom: 4rem !important;
}

/* Mobile : empiler proprement */
@media (max-width: 900px) {
  #page-contact .contact-grid {
    grid-template-columns: 1fr !important;
    gap: 28px !important;
  }

  #page-contact .contact-photo {
    max-width: 360px !important;
  }

  #page-contact .contact-photo img {
    max-height: 460px !important;
  }
}

/* Form : espacement propre */
#page-contact form {
  margin-top: 14px !important;
}

#page-contact .form-row,
#page-contact .form-group {
  margin-bottom: 18px !important;
}

#page-contact input,
#page-contact textarea {
  padding: 12px 10px !important;
  width: 100% !important;
  background: #fff !important;
}

/* Bouton envoyer : plus “premium”, sans changer le style global du site */
#page-contact button,
#page-contact .btn,
#page-contact .btn-outline {
  letter-spacing: 0.12em !important;
  text-transform: uppercase;
}

/* FAQ : arrêter l’effet “trop étiré” */
#page-contact .faq,
#page-contact .faq * {
  line-height: 1.55 !important;
}

#page-contact .faq h3,
#page-contact .faq h4 {
  margin: 18px 0 10px 0 !important;
}

#page-contact .faq p {
  margin: 0 0 8px 0 !important;
}

#page-contact .faq-item {
  padding: 8px 0 !important;
}

/* --- CONTACT: SOCIAL & INFO LINKS WITH ICONS --- */
#page-contact .infos {
  display: flex !important;
  flex-direction: column !important;
  gap: 1.2rem !important;
  margin-top: 25px !important;
  border-top: 1px solid rgba(255, 255, 255, 0.1) !important;
  padding-top: 20px !important;
  text-align: left !important;
}

.info-item {
  display: flex !important;
  align-items: center !important;
}

.info-link,
.info-static {
  display: flex !important;
  align-items: center !important;
  gap: 15px !important;
  color: #fff !important;
  opacity: 0.75;
  transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1) !important;
  font-family: var(--font-main) !important;
  font-size: 1rem !important;
  text-decoration: none !important;
  line-height: 1 !important;
  margin: 0 !important;
}

.info-link:hover {
  opacity: 1 !important;
  color: #fff !important;
  transform: translateX(8px) !important;
}

.info-icon {
  width: 20px !important;
  height: 20px !important;
  stroke: currentColor;
  stroke-width: 1.5;
  flex-shrink: 0;
  display: block;
}

#page-contact .infos p {
  display: none !important;
  /* Hide old generic paragraphs if any remain */
}

/* Sécurité Responsive Container */
@media (max-width: 900px) {
  #page-contact .container {
    padding-left: 18px !important;
    padding-right: 18px !important;
  }
}




/* --- ACCUEIL : FIX TITRES ÉDITORIAUX (REV 2) --- */
.home-editorial {
  background: transparent !important;
  /* Ne pas forcer le fond ici */
}

.home-editorial h1,
.home-editorial h2,
.home-editorial h3 {
  display: block !important;
  visibility: visible !important;
  opacity: 1 !important;
  transform: none !important;
}

.home-editorial,
.home-editorial h1,
.home-editorial h2,
.home-editorial h3,
.home-editorial p {
  font-family: inherit !important;
  color: inherit !important;
}



/* --- VINYLES : BANDEAU VIDÉO FULL-BLEED (FIX RADICAL PLAN B) --- */
.vinyl-page {
  transform: none !important;
  filter: none !important;
  perspective: none !important;
}

.vinyl-video-band {
  width: 100vw !important;
  height: 60vh !important;
  position: relative !important;
  left: 50% !important;
  transform: translateX(-50%) !important;
  margin: 0 !important;
  padding: 0 !important;
  overflow: hidden !important;
  display: block !important;

  /* Sécurité Grid Parent */
  grid-column: 1 / -1 !important;
  justify-self: stretch !important;
  align-self: stretch !important;
}

@media (max-width: 900px) {
  .vinyl-video-band {
    height: 45vh !important;
  }
}

.vinyl-video-band video {
  width: 100% !important;
  height: 100% !important;
  display: block !important;
  object-fit: cover !important;
}

html,
body {
  overflow-x: hidden !important;
}

/* --- VINYLES : EFFET HOVER SUBTIL --- */
.vinyl-hover-subtle {
  transition: transform 1.2s cubic-bezier(0.2, 0, 0.2, 1), filter 1.2s ease !important;
  cursor: pointer;
}

.vinyl-hover-subtle:hover {
  transform: scale(1.025) !important;
  filter: brightness(1.04) !important;
}

/* --- ARTISTE : EFFET HOVER ULTRA-SUBTIL --- */
.artist-hover-subtle {
  transition: transform 1.5s cubic-bezier(0.2, 0, 0.2, 1), filter 1.5s ease !important;
  cursor: pointer;
  pointer-events: auto !important;
}

.artist-hover-subtle:hover {
  transform: scale(1.015) translate(-50%, 50%) !important;
  filter: brightness(1.03) !important;
}

/* --- KEN BURNS SLIDESHOW (TABLEAUX PAGE) --- */
.kb-slideshow {
  position: relative;
  width: 100%;
  aspect-ratio: 1/1;
  overflow: hidden;
  background: #000;
  display: block;
}

.kb-slideshow .kb-slide {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  background-size: cover;
  background-position: center;
  transition: opacity 1.5s ease-in-out;
  animation: kb-loop 12s ease-in-out infinite;
}

.kb-slideshow .kb-slide:nth-child(2) {
  animation-delay: 6s;
}



@keyframes kb-loop {
  0% {
    opacity: 0;
    transform: scale(1);
  }

  12.5% {
    /* 1.5s - Fully visible */
    opacity: 1;
  }

  50% {
    /* 6s - Start fade out */
    opacity: 1;
  }

  62.5% {
    /* 7.5s - Fully invisible */
    opacity: 0;
    transform: scale(1.04);
  }

  100% {
    opacity: 0;
    transform: scale(1);
  }
}



@keyframes kb-simple-burns {
  0% {
    transform: scale(1);
  }

  50% {
    transform: scale(1.08);
  }

  100% {
    transform: scale(1);
  }
}

/* 4-Image Version for Tableaux Bottom Section */
.kb-slideshow-4 {
  position: relative;
  width: 100%;
  aspect-ratio: 1/1;
  overflow: hidden;
  background: #000;
  display: block;
}

.kb-slideshow-4 .kb-slide {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  background-size: cover;
  background-position: center;
  animation: kb-loop-4 12s ease-in-out infinite;
}

.kb-slideshow-4 .kb-slide:nth-child(1) {
  animation-delay: 0s;
}

.kb-slideshow-4 .kb-slide:nth-child(2) {
  animation-delay: 3s;
}

.kb-slideshow-4 .kb-slide:nth-child(3) {
  animation-delay: 6s;
}

.kb-slideshow-4 .kb-slide:nth-child(4) {
  animation-delay: 9s;
}

@keyframes kb-loop-4 {
  0% {
    opacity: 0;
    transform: scale(1);
  }

  /* Fade in sur 1s (8.33% de 12s) */
  8.33% {
    opacity: 1;
  }

  /* Reste visible jusqu'à l'apparition de la suivante à 3s (25%) */
  25% {
    opacity: 1;
  }

  /* Fondu sortant croisé avec la suivante (finit à 4s ≈ 33.33%) */
  33.33% {
    opacity: 0;
    transform: scale(1.04);
  }

  100% {
    opacity: 0;
  }
}

/* 6-Image Version for Artist Overlap Section */
.kb-slideshow-6 {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
  background: #000;
  display: block;
}

.kb-slideshow-6 .kb-slide {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  background-size: cover;
  background-position: center;
  animation: kb-loop-6 18s ease-in-out infinite;
}

.kb-slideshow-6 .kb-slide:nth-child(1) {
  animation-delay: 0s;
}

.kb-slideshow-6 .kb-slide:nth-child(2) {
  animation-delay: 3s;
}

.kb-slideshow-6 .kb-slide:nth-child(3) {
  animation-delay: 6s;
}

.kb-slideshow-6 .kb-slide:nth-child(4) {
  animation-delay: 9s;
}

.kb-slideshow-6 .kb-slide:nth-child(5) {
  animation-delay: 12s;
}

.kb-slideshow-6 .kb-slide:nth-child(6) {
  animation-delay: 15s;
}

/* --- ARTISTE : MINI-FILM ORGANIQUE (CROSS-FADE CONTINU ET ASYMÉTRIQUE) --- */
.organic-mini-film {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
  background: #000 !important;
  /* Fond noir de sécurité mais caché par le cross-fade */
}

.organic-mini-film .kb-slide {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  background-size: cover;
  background-position: center;
  animation-duration: 13.2s;
  /* Cycle précis pour 1s / 2s / 0.7s + 0.8s transitions */
  animation-iteration-count: infinite;
  animation-timing-function: linear;
}

.organic-mini-film .kb-slide:nth-child(1) {
  animation-name: kb-org-1;
  opacity: 1;
  /* Force visibility on load to prevent flash */
}

.organic-mini-film .kb-slide:nth-child(2) {
  animation-name: kb-org-2;
}

.organic-mini-film .kb-slide:nth-child(3) {
  animation-name: kb-org-3;
}

.organic-mini-film .kb-slide:nth-child(4) {
  animation-name: kb-org-4;
}

.organic-mini-film .kb-slide:nth-child(5) {
  animation-name: kb-org-5;
}

.organic-mini-film .kb-slide:nth-child(6) {
  animation-name: kb-org-6;
}

/* Img 1: 1s display + transition out. Fades in at end of loop. */
@keyframes kb-org-1 {
  0% {
    opacity: 1;
    transform: scale(1);
  }

  2% {
    opacity: 1;
    transform: scale(1);
  }

  /* Brief movement delay after load/loop */
  7.57% {
    opacity: 1;
    transform: scale(1.01);
  }

  /* 1.0s display point */
  13.63% {
    opacity: 0;
    transform: scale(1.02);
  }

  /* 0.8s fade out */
  93.93% {
    opacity: 0;
    transform: scale(0.99);
  }

  /* start fade in */
  100% {
    opacity: 1;
    transform: scale(1);
  }
}

/* Img 2: 2s display. Starts 1s in. */
@keyframes kb-org-2 {
  0% {
    opacity: 0;
  }

  7.57% {
    opacity: 0;
    transform: scale(1);
  }

  13.63% {
    opacity: 1;
    transform: scale(1.01);
  }

  28.78% {
    opacity: 1;
    transform: scale(1.02);
  }

  /* 2.0s display */
  34.84% {
    opacity: 0;
    transform: scale(1.03);
  }

  /* 0.8s fade out */
  100% {
    opacity: 0;
  }
}

/* Img 3: 2s display. Starts 3.8s in. */
@keyframes kb-org-3 {
  0% {
    opacity: 0;
  }

  28.78% {
    opacity: 0;
    transform: scale(1);
  }

  34.84% {
    opacity: 1;
    transform: scale(1.01);
  }

  50.00% {
    opacity: 1;
    transform: scale(1.02);
  }

  56.06% {
    opacity: 0;
    transform: scale(1.03);
  }

  100% {
    opacity: 0;
  }
}

/* Img 4: 2s display. Starts 6.6s in. */
@keyframes kb-org-4 {
  0% {
    opacity: 0;
  }

  50.00% {
    opacity: 0;
    transform: scale(1);
  }

  56.06% {
    opacity: 1;
    transform: scale(1.01);
  }

  71.21% {
    opacity: 1;
    transform: scale(1.02);
  }

  77.27% {
    opacity: 0;
    transform: scale(1.03);
  }

  100% {
    opacity: 0;
  }
}

/* Img 5: 0.7s display. Starts 9.4s in. */
@keyframes kb-org-5 {
  0% {
    opacity: 0;
  }

  71.21% {
    opacity: 0;
    transform: scale(1.01);
  }

  77.27% {
    opacity: 1;
    transform: scale(1.01);
  }

  82.57% {
    opacity: 1;
    transform: scale(1.015);
  }

  /* 0.7s display */
  88.63% {
    opacity: 0;
    transform: scale(1.02);
  }

  /* 0.8s fade out */
  100% {
    opacity: 0;
  }
}

/* Img 6: 0.7s display. Starts 10.9s in. Transitions to Img 1. */
@keyframes kb-org-6 {
  0% {
    opacity: 0;
  }

  82.57% {
    opacity: 0;
    transform: scale(1);
  }

  88.63% {
    opacity: 1;
    transform: scale(1.01);
  }

  93.93% {
    opacity: 1;
    transform: scale(1.015);
  }

  100% {
    opacity: 0;
    transform: scale(1.02);
  }
}

/* --- ARTISTE : MINI-FILM ORGANIQUE (VERSION 3 IMAGES - ASYMÉTRIQUE) --- */
.organic-mini-film-3 {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
  background: #000 !important;
}

.organic-mini-film-3 .kb-slide {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  background-size: cover;
  background-position: center;
  animation-duration: 5.8s;
  animation-iteration-count: infinite;
  animation-timing-function: linear;
}

.organic-mini-film-3 .kb-slide:nth-child(1) {
  animation-name: kb-org3-1;
  opacity: 1;
}

.organic-mini-film-3 .kb-slide:nth-child(2) {
  animation-name: kb-org3-2;
}

.organic-mini-film-3 .kb-slide:nth-child(3) {
  animation-name: kb-org3-3;
}

/* Img 1: 2s display + 0.8s fade out */
@keyframes kb-org3-1 {
  0% {
    opacity: 1;
    transform: scale(1);
  }

  34.48% {
    opacity: 1;
    transform: scale(1.015);
  }

  /* 2s display */
  48.27% {
    opacity: 0;
    transform: scale(1.025);
  }

  /* 0.8s fade out */
  86.21% {
    opacity: 0;
    transform: scale(0.99);
  }

  /* start fade in */
  100% {
    opacity: 1;
    transform: scale(1);
  }
}

/* Img 2: 0.7s display. Starts 2s in. */
@keyframes kb-org3-2 {
  0% {
    opacity: 0;
  }

  34.48% {
    opacity: 0;
    transform: scale(1);
  }

  48.27% {
    opacity: 1;
    transform: scale(1.01);
  }

  60.34% {
    opacity: 1;
    transform: scale(1.015);
  }

  /* 0.7s display */
  74.13% {
    opacity: 0;
    transform: scale(1.025);
  }

  /* 0.8s fade out */
  100% {
    opacity: 0;
  }
}

/* Img 3: 0.7s display. Starts 3.5s in. Transitions to 1. */
@keyframes kb-org3-3 {
  0% {
    opacity: 0;
  }

  60.34% {
    opacity: 0;
    transform: scale(1);
  }

  74.13% {
    opacity: 1;
    transform: scale(1.01);
  }

  86.21% {
    opacity: 1;
    transform: scale(1.015);
  }

  /* 0.7s display */
  100% {
    opacity: 0;
    transform: scale(1.025);
  }

  /* 0.8s fade out */
}

/* ============================================================ */
/* ARTISTE — CAROUSEL MÉDIAS PREMIUM                         */
/* ============================================================ */

#page-artiste .artist-carousel-wrapper {
  position: relative !important;
  width: 100% !important;
  height: 400px !important;
  /* Augmenté pour plus de visibilité (+33%) */
  overflow: hidden !important;
  background: #000 !important;
  margin: 0 !important;
  padding: 0 !important;
}

#page-artiste .artist-carousel-content {
  display: flex !important;
  height: 100% !important;
  width: max-content !important;
  padding: 0 !important;
  margin: 0 !important;
  background: #000 !important;
  will-change: transform;
}

#page-artiste .artist-carousel-content::-webkit-scrollbar {
  display: none !important;
}

#page-artiste .carousel-item {
  flex: 0 0 auto !important;
  width: 300px !important;
  /* Largeur fixe pour l'harmonie */
  height: 100% !important;
  margin: 0 25px 0 0 !important;
  padding: 0 !important;
  position: relative !important;
  overflow: hidden !important;
  background: #000 !important;
  /* Fond noir pour "avaler" les bords */
  border: none !important;
  outline: none !important;
}

#page-artiste .carousel-item img,
#page-artiste .carousel-item video {
  width: 100% !important;
  height: 100% !important;
  object-fit: cover !important;
  /* Élimine radicalement les bandes blanches */
  display: block !important;
  background: #000 !important;
  border: none !important;
  outline: none !important;
  transition: transform 0.6s cubic-bezier(0.23, 1, 0.32, 1) !important;
  cursor: pointer !important;
}




/* Feedback Premium : Effet "Pop" + Focus (Renforcé) */
#page-artiste .carousel-item.is-popped {
  z-index: 100 !important;
  transform: scale(1.08) !important;
  box-shadow: 0 30px 60px rgba(0, 0, 0, 0.5) !important;
  transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275) !important;
}

/* --- CAROUSEL CONTROLS --- */
.artist-carousel-controls {
  position: absolute;
  bottom: 25px;
  left: 50%;
  transform: translateX(-50%);
  display: flex !important;
  align-items: center;
  gap: 15px;
  z-index: 1000;
  background: rgba(0, 0, 0, 0.2);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  padding: 8px 15px;
  border-radius: 40px;
  opacity: 0;
  transition: opacity 0.3s ease, transform 0.3s ease;
}

.artist-carousel-wrapper:hover .artist-carousel-controls {
  opacity: 1;
}

.carousel-control-btn {
  background: transparent;
  color: #fff;
  cursor: pointer;
  display: flex !important;
  align-items: center;
  justify-content: center;
  width: 36px;
  height: 36px;
  border-radius: 50%;
  transition: background 0.2s, transform 0.2s;
  padding: 0;
}

.carousel-control-btn:hover {
  background: rgba(255, 255, 255, 0.15);
  transform: scale(1.1);
}

.carousel-control-btn:active {
  transform: scale(0.9);
}

.carousel-control-btn svg {
  width: 20px;
  height: 20px;
}

.btn-main-control {
  width: 44px;
  height: 44px;
  background: rgba(255, 255, 255, 0.1);
}

#page-artiste .carousel-item.is-popped img,
#page-artiste .carousel-item.is-popped video {
  transform: scale(1.07) !important;
  /* Pop plus visible ≈ +7% */
  z-index: 10 !important;
}

/* Navigation Carousel removed for cleaner défilé */

/* Mobile Adjustments */
@media (max-width: 768px) {
  #page-artiste .carousel-item {
    min-width: 80vw !important;
  }
}


/* --- ARTIST PAGE PREMIUM REFINEMENT --- */

/* Hero Band Full Width (Constraint B) - Adjusted Height */
.artist-hero-band {
  width: 100% !important;
  height: 65vh !important;
  min-height: 320px !important;
  max-height: 700px !important;
  position: relative !important;
  overflow: hidden !important;
  background: #000 !important;
}







.artist-hero-band img {
  width: 100% !important;
  height: 100% !important;
  object-fit: cover !important;
  object-position: center 40% !important;
  display: block !important;
}






/* Floating Profile Card - Square & Symmetrical */
.artist-profile-card {
  position: relative !important;
  z-index: 25 !important;
  width: 380px !important;
  height: 380px !important;
  margin: -180px auto 0 !important;
  background: #fff !important;
  border-radius: 4px !important;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1) !important;
  overflow: hidden !important;
}






.artist-profile-card img {
  width: 100% !important;
  height: 100% !important;
  object-fit: cover !important;
  object-position: center 15% !important;


}



/* Bande Photo Intermédiaire (Plein Largeur) */
.artist-img-band-full {
  width: 100% !important;
  height: 55vh !important;
  min-height: 380px !important;
  max-height: 600px !important;
  overflow: hidden !important;
  margin: 0 !important;
  /* Bord à bord - No fades */
}


.artist-img-band-full img {
  width: 100% !important;
  height: 100% !important;
  object-fit: cover !important;
  display: block !important;
}


/* --- ARTIST EDITORIAL SYSTEM (Magazine Luxe Def) --- */
.artist-editorial-section {
  padding: 80px 1rem !important;
  position: relative !important;
  display: block !important;
  z-index: 10 !important;
  opacity: 1 !important;
  background: transparent;
}

.editorial-col {
  max-width: 800px !important;
  width: min(800px, calc(100% - 48px)) !important;
  margin: 0 auto !important;
  text-align: justify !important;
}

/* Section Noire Intro Spécifique */
.artist-section-dark {
  background: #0E0E0E !important;
}

.section-light.artist-editorial-section {
  background: #ffffff !important;
  text-align: justify !important;
}

.section-light.artist-editorial-section .editorial-col {
  max-width: 800px !important;
  width: min(800px, calc(100% - 48px)) !important;
  margin: 0 auto !important;
  text-align: justify !important;
}



/* Les styles typographiques sont désormais gérés globalement pour éviter les différences entre les pages. */





@media (max-width: 768px) {
  .artist-editorial-section {
    padding: 40px 1rem !important;
  }

  .editorial-col {
    margin-left: 20px !important;
    width: calc(100% - 40px) !important;
  }

  .artist-editorial-title-large {
    font-size: 20px !important;
  }
}

/* --- RECIPE LINKS (PREMIUM) --- */
.artist-link {
  color: #fff !important;
  text-decoration: none !important;
  font-weight: 500 !important;
  font-style: italic;
  border-bottom: 1px solid rgba(255, 255, 255, 0.3) !important;
  padding-bottom: 2px;
  transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1) !important;
}

.artist-link:hover {
  color: var(--accent) !important;
  border-bottom-color: var(--accent) !important;
  background: rgba(255, 255, 255, 0.05);
  padding: 2px 4px;
  border-radius: 2px;
}

.section-light .artist-link {
  text-decoration-color: rgba(0, 0, 0, 0.2) !important;
}

.section-light .artist-link:hover {
  text-decoration-color: #000 !important;
}

/* --- NAV DROPDOWN --- */
.nav-item-dropdown {
  position: relative;
}

.nav-item-dropdown:hover .dropdown-menu {
  opacity: 1;
  visibility: visible;
  transform: translateX(-50%) translateY(0);
}

.dropdown-menu {
  position: absolute;
  top: 100%;
  left: 50%;
  transform: translateX(-50%) translateY(10px);
  background: #ffffff;
  min-width: 180px;
  max-height: 400px;
  overflow-y: auto;
  padding: 1rem 0;
  border-radius: 4px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
  list-style: none;
  z-index: 1100;
  display: flex !important;
  flex-direction: column !important;
  gap: 0 !important;
}

/* Custom scrollbar for dropdown */
.dropdown-menu::-webkit-scrollbar {
  width: 4px;
}

.dropdown-menu::-webkit-scrollbar-track {
  background: #f1f1f1;
}

.dropdown-menu::-webkit-scrollbar-thumb {
  background: #ccc;
  border-radius: 2px;
}

.dropdown-menu li {
  width: 100%;
  margin: 0 !important;
  padding: 0 !important;
}

.dropdown-menu a {
  display: block !important;
  padding: 0.7rem 1.5rem !important;
  font-family: var(--font-main) !important;
  font-size: 0.7rem !important;
  color: #000 !important;
  text-transform: uppercase !important;
  letter-spacing: 0.1em !important;
  transition: background 0.2s !important;
  text-decoration: none !important;
  opacity: 0.8 !important;
  text-align: left !important;
}

.dropdown-menu a:hover {
  background: #f8f8f8 !important;
  opacity: 1 !important;
}

@media (max-width: 900px) {
  .dropdown-menu {
    display: none !important;
  }
}

/* --- PREMIUM MODAL CLOSE BUTTON --- */
.modal-close {
  position: absolute;
  top: 10px;
  right: 15px;
  width: 42px;
  height: 42px;
  display: flex !important;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  color: #ffffff;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 50%;
  transition: all 0.3s cubic-bezier(0.165, 0.84, 0.44, 1);
  z-index: 10005;
  border: 1px solid rgba(255, 255, 255, 0.15);
}

.modal-close:hover {
  background: rgba(255, 255, 255, 0.25);
  transform: rotate(90deg) scale(1.1);
  border-color: rgba(255, 255, 255, 0.4);
}

.modal-close svg {
  width: 18px;
  height: 18px;
  display: block;
}

@media (max-width: 900px) {
  .modal-close {
    top: 5px;
    right: 10px;
  }
}

@media (max-width: 600px) {
  .modal-close {
    top: 32px;
    right: 20px;
    width: 38px;
    height: 38px;
    background: rgba(0, 0, 0, 0.6);
    border-color: rgba(255, 255, 255, 0.2);
  }
}

/* --- HARMONISATION TYPO MOBILE (Correction Justification) --- */
@media (max-width: 900px) {

  p,
  .editorial-col,
  .artist-editorial-section,
  .contact-right p,
  .faq-item p {
    text-align: left !important;
    hyphens: auto;
    /* Aide à la lisibilité sur écrans étroits */
  }
}